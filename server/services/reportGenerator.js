const fs = require("fs");
const path = require("path");

const REPORT_DIR = path.join(__dirname, "..", "..", "automation-system", "artifacts", "reports");

function esc(value) { return String(value ?? "").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#039;"); }
function pct(passed,total){ return total ? Math.round((passed/total)*100) : 0; }
function tcIdFromTitle(title){ return String(title||"").match(/TC(?:\d{3}|-H\d{3})/)?.[0] || "—"; }
function evidenceLinks(test){ if(!test.fail)return "—";const links=[];if(test.evidence?.videoUrl)links.push(`<a class="evidence-link" href="${esc(test.evidence.videoUrl)}" target="_blank" rel="noopener">▶ Video</a>`);if(test.evidence?.screenshotUrl)links.push(`<a class="evidence-link" href="${esc(test.evidence.screenshotUrl)}" target="_blank" rel="noopener">▣ Screenshot</a>`);return links.length?`<div class="evidence-links">${links.join("")}</div>`:"—"; }
function ownerLabel(value){const labels={APPLICATION_TEAM:"Application team",TEST_AUTOMATION_TEAM:"Test automation team",TEST_DATA_OWNER:"Test data owner",ENVIRONMENT_TEAM:"Environment / DevOps team",BUSINESS_ANALYST:"Business analyst / product owner",MANUAL_REVIEW:"Manual review"};return labels[value]||String(value||"Manual review").replaceAll("_"," ");}

function sourceHtml(analysis){
  const level=String(analysis?.sourceGuidanceLevel||"BLACK_BOX");
  const files=Array.isArray(analysis?.sourceCandidateFiles)?analysis.sourceCandidateFiles:[];
  if(level==="BLACK_BOX"||!files.length)return `<div class="source-level blackbox">BLACK BOX</div>`;
  const cls=level==="SOURCE_VERIFIED"?"verified":"suggested";
  return `<div class="source-level ${cls}">${esc(level.replaceAll("_"," "))}</div><div class="source-files">${files.map(file=>{
    const range=file.startLine?` · lines ${esc(file.startLine)}${file.endLine?`-${esc(file.endLine)}`:""}`:"";
    return `<div class="source-file"><code>${esc(file.path)}</code>${range}${file.reason?`<span>${esc(file.reason)}</span>`:""}</div>`;
  }).join("")}</div>`;
}

function pendingAnalysisHtml(test){
  if(!test?.fail)return "—";
  return `<div class="analysis-live pending"><div class="analysis-live-head"><span class="analysis-live-dot"></span><strong>Waiting for AI response…</strong></div><div class="analysis-live-note">Execution has finished. This failed case is queued for failed-only AI analysis; the PASS/FAIL result and evidence remain unchanged.</div></div>`;
}

function analysisHtml(analysis,test){
  if(!analysis)return pendingAnalysisHtml(test);
  const classificationClass=analysis.classification==="APPLICATION_DEFECT"?"classification defect":analysis.classification==="AUTOMATION_DEFECT"?"classification automation":"classification";
  const details=[];if(analysis.expected)details.push(`<div class="analysis-detail"><b>Expected:</b> ${esc(analysis.expected)}</div>`);if(analysis.actual)details.push(`<div class="analysis-detail"><b>Observed:</b> ${esc(analysis.actual)}</div>`);if(analysis.probableCause)details.push(`<div class="analysis-detail"><b>Probable cause:</b> ${esc(analysis.probableCause)}</div>`);
  let developer="";
  if(analysis.classification==="APPLICATION_DEFECT"){
    const regression=Array.isArray(analysis.regressionChecks)&&analysis.regressionChecks.length?`<ol>${analysis.regressionChecks.map(step=>`<li>${esc(step)}</li>`).join("")}</ol>`:"";
    developer=`<div class="developer-box"><div class="developer-head">Developer fix suggestion</div>${sourceHtml(analysis)}${analysis.developerReviewArea?`<div class="analysis-detail"><b>Where to inspect:</b> ${esc(analysis.developerReviewArea)}</div>`:""}${analysis.developerImplementationHint?`<div class="analysis-detail"><b>Implementation hint:</b> ${esc(analysis.developerImplementationHint)}</div>`:""}${analysis.developerExampleFix?`<div class="analysis-detail"><b>Illustrative / proposed fix:</b></div><pre>${esc(analysis.developerExampleFix)}</pre>`:""}${regression?`<div class="analysis-detail"><b>Regression checks:</b>${regression}</div>`:""}<div class="developer-warning">SOURCE_VERIFIED means the candidate path/line area was grounded in repository evidence supplied to AI. It is still advisory and not an applied or proven patch.</div></div>`;
  }
  const verification=Array.isArray(analysis.verificationSteps)&&analysis.verificationSteps.length?`<ol>${analysis.verificationSteps.map(step=>`<li>${esc(step)}</li>`).join("")}</ol>`:"";
  const resolution=`<div class="resolution-box"><div class="resolution-head">AI resolution guidance <span>Human review required</span></div>${analysis.resolutionComment?`<div class="resolution-comment">${esc(analysis.resolutionComment)}</div>`:""}${analysis.recommendedFix?`<div class="analysis-detail"><b>Recommended fix:</b> ${esc(analysis.recommendedFix)}</div>`:""}${analysis.recommendedOwner?`<div class="analysis-detail"><b>Suggested owner:</b> ${esc(ownerLabel(analysis.recommendedOwner))}</div>`:""}${developer}${verification?`<div class="analysis-detail"><b>Verify after correction:</b>${verification}</div>`:""}<div class="resolution-warning">Advisory only — this does not modify application code, weaken the test, or prove resolution. Resolution is proven by a successful re-run of the original approved test.</div></div>`;
  return `<span class="${classificationClass}">${esc(analysis.classification)}</span><div class="analysis-summary">${esc(analysis.summary||"")}</div>${details.join("")}${resolution}`;
}

function reportFileName(sessionId){const safe=String(sessionId||"run").replace(/[^a-zA-Z0-9_-]/g,"_");return `${safe}.html`;}
function saveReportHtml(sessionId,html){fs.mkdirSync(REPORT_DIR,{recursive:true});const filePath=path.join(REPORT_DIR,reportFileName(sessionId));fs.writeFileSync(filePath,html,"utf8");return filePath;}

function buildAnalyticsReport({sessionId,story,targetUrl,environment,summary,analyses}){
  const successRate=pct(summary.passed,summary.total),defectCount=(analyses||[]).filter(a=>a?.classification==="APPLICATION_DEFECT").length,analysisById=new Map((analyses||[]).map(a=>[String(a.testCase||"").toUpperCase(),a]));
  const rows=(summary.tests||[]).map(t=>{const id=tcIdFromTitle(t.title),analysis=analysisById.get(String(id).toUpperCase()),defectDetected=!t.pass&&analysis?.classification==="APPLICATION_DEFECT";return `<tr><td><code>${esc(id)}</code></td><td>${esc(t.title)}</td><td><span class="status ${t.pass?"pass":"fail"}">${t.pass?"PASS":"FAIL"}</span>${defectDetected?'<div class="detected">Application defect detected</div>':''}</td><td>${t.durationMs==null?"—":`${esc(t.durationMs)} ms`}</td><td>${evidenceLinks(t)}</td><td class="analysis-cell" data-analysis-case="${esc(id)}" data-analysis-failed="${t.fail?"true":"false"}">${analysisHtml(analysis,t)}</td></tr>`;}).join("");
  const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>AI TestPilot Analytics</title><style>
  :root{--bg:#f6f8fb;--panel:#fff;--border:#e6eaf1;--text:#10141c;--muted:#6b7385;--blue:#2f5bff;--green:#15803d;--red:#b91c1c;--amber:#92400e}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:Inter,Segoe UI,Arial,sans-serif}.wrap{max-width:1480px;margin:0 auto;padding:30px 22px 50px}.hero{display:flex;justify-content:space-between;gap:24px;align-items:flex-start;margin-bottom:24px}.hero-right{display:flex;align-items:flex-start;gap:12px}.hero-actions{display:flex;gap:8px;flex-wrap:wrap}.export-btn{border:1px solid #c7d2fe;background:#eef2ff;color:#3730a3;border-radius:9px;padding:9px 12px;font-size:11px;font-weight:800;cursor:pointer;white-space:nowrap}.export-btn:hover{border-color:#8098ff;background:#e0e7ff}h1{margin:0 0 6px;font-size:28px}.muted{color:var(--muted);font-size:13px;line-height:1.45}.meta{font-size:12px;color:var(--muted);text-align:right}.cards{display:grid;grid-template-columns:repeat(5,1fr);gap:14px;margin:20px 0}.card{background:var(--panel);border:1px solid var(--border);border-radius:14px;padding:18px}.metric{font-size:30px;font-weight:800;margin-top:6px}.label{font-size:12px;color:var(--muted)}.story{white-space:pre-wrap;line-height:1.55}.section-title{margin:26px 0 10px;font-size:16px}table{width:100%;border-collapse:collapse;background:#fff;border:1px solid var(--border)}th,td{padding:12px 13px;border-bottom:1px solid var(--border);text-align:left;vertical-align:top;font-size:13px}th{background:#fafbfd;color:var(--muted);font-size:11px;text-transform:uppercase}.status,.classification,.source-level{display:inline-block;padding:4px 8px;border-radius:999px;font-weight:800;font-size:10px}.pass{background:#dcfce7;color:var(--green)}.fail{background:#fee2e2;color:var(--red)}.classification{background:#eef2ff;color:#3730a3}.classification.defect{background:#fee2e2;color:var(--red)}.classification.automation{background:#fef3c7;color:var(--amber)}.detected{margin-top:5px;font-size:11px;font-weight:700;color:var(--red)}code{font-family:Consolas,monospace;color:var(--blue)}.evidence-links{display:flex;gap:7px;flex-wrap:wrap}.evidence-link{text-decoration:none;border:1px solid #dbe3ff;background:#f5f7ff;color:var(--blue);padding:5px 8px;border-radius:7px;font-size:11px;font-weight:700}.readiness-note{border-left:4px solid var(--blue);background:#f5f7ff}.analysis-summary{margin-top:7px;line-height:1.45;color:var(--muted)}.analysis-detail{margin-top:6px;line-height:1.4;font-size:12px}.resolution-box,.developer-box{margin-top:10px;padding:11px 12px;border-radius:9px;border:1px solid #bfdbfe;background:#f8fbff}.developer-box{background:#fff;border-color:#c7d2fe}.resolution-head,.developer-head{font-size:11px;font-weight:900;color:#1d4ed8;text-transform:uppercase}.developer-head{color:#4338ca}.resolution-head span{margin-left:6px;padding:2px 5px;border-radius:5px;background:#fef3c7;color:#92400e;font-size:9px}.resolution-comment{margin-top:7px;font-size:12px}.resolution-box ol,.developer-box ol{margin:5px 0 0 18px;padding:0}.resolution-warning,.developer-warning{margin-top:9px;padding-top:7px;border-top:1px solid #dbeafe;color:#64748b;font-size:10.5px}.developer-box pre{margin:7px 0 0;padding:10px;border-radius:7px;background:#111827;color:#e5e7eb;font-family:Consolas,monospace;font-size:11px;white-space:pre-wrap;overflow:auto}.source-level{margin-top:7px}.source-level.verified{background:#dcfce7;color:#166534}.source-level.suggested{background:#fef3c7;color:#92400e}.source-level.blackbox{background:#e5e7eb;color:#475569}.source-files{margin-top:7px;border:1px solid #dbeafe;border-radius:8px;overflow:hidden}.source-file{padding:7px 9px;border-bottom:1px solid #eef2ff;font-size:10.5px;background:#f8fafc}.source-file:last-child{border-bottom:0}.source-file span{display:block;color:#64748b;margin-top:2px}.analysis-live{padding:11px 12px;border-radius:9px;border:1px solid #dbeafe;background:#f8fbff}.analysis-live.running{border-color:#bfdbfe;background:#eff6ff}.analysis-live.error{border-color:#fecaca;background:#fff7f7}.analysis-live-head{display:flex;align-items:center;gap:8px;font-size:11px;color:#1d4ed8}.analysis-live.error .analysis-live-head{color:#b91c1c}.analysis-live-note{margin-top:6px;font-size:11px;line-height:1.45;color:#64748b}.analysis-live-dot{width:8px;height:8px;border-radius:999px;background:#3b82f6;box-shadow:0 0 0 0 rgba(59,130,246,.35);animation:analysisPulse 1.4s infinite}@keyframes analysisPulse{0%{box-shadow:0 0 0 0 rgba(59,130,246,.35)}70%{box-shadow:0 0 0 7px rgba(59,130,246,0)}100%{box-shadow:0 0 0 0 rgba(59,130,246,0)}}@media(max-width:1050px){.cards{grid-template-columns:repeat(3,1fr)}}@media(max-width:800px){.cards{grid-template-columns:repeat(2,1fr)}.hero{display:block}.hero-right{margin-top:14px;justify-content:space-between}.meta{text-align:left}table{display:block;overflow:auto}}
  </style></head><body data-report-session-id="${esc(sessionId)}"><div class="wrap"><div class="hero"><div><h1>AI TestPilot Analytics</h1><div class="muted">Story-driven deterministic Cypress execution with optional source-aware AI remediation</div></div><div class="hero-right"><div class="hero-actions"><button id="exportAnalysisExcel" class="export-btn" type="button">Export Excel</button></div><div class="meta">Run: ${esc(sessionId)}<br>Generated: ${esc(new Date().toLocaleString())}</div></div></div><div class="cards"><div class="card"><div class="label">Tests</div><div class="metric">${esc(summary.total)}</div></div><div class="card"><div class="label">Passed</div><div class="metric" style="color:var(--green)">${esc(summary.passed)}</div></div><div class="card"><div class="label">Failed</div><div class="metric" style="color:var(--red)">${esc(summary.failed)}</div></div><div class="card"><div class="label">Defects detected</div><div id="defectMetric" class="metric" style="color:var(--red)">${esc(defectCount)}</div></div><div class="card"><div class="label">Execution pass rate</div><div class="metric" style="color:var(--blue)">${successRate}%</div></div></div><div class="card readiness-note"><strong>Automation Ready ≠ Test Passed</strong><div class="muted">Readiness proves deterministic executability. PASS/FAIL is determined by browser execution. Source-aware AI runs after failure and is advisory.</div></div><div class="card" style="margin-top:14px"><div class="label">Target</div><strong>${esc(targetUrl)}</strong><div class="muted" style="margin-top:4px">Environment: ${esc(environment||"Test")}${summary.browser?` · Browser: ${esc(summary.browser)}`:""}</div></div><h2 class="section-title">Business story</h2><div class="card story">${esc(story)}</div><h2 class="section-title">Execution results</h2><table><thead><tr><th>Case</th><th>Test</th><th>Execution outcome</th><th>Duration</th><th>Evidence</th><th>Failure analysis & source-aware developer guidance</th></tr></thead><tbody>${rows}</tbody></table><div class="muted" style="margin-top:12px">Only failed cases are eligible for AI failure analysis. SOURCE_VERIFIED indicates repository-grounded candidate evidence, not an automatically applied fix. Human review and a successful re-run remain required.</div></div><script src="/report-live-analysis.js"></script><script src="/report-excel.js"></script></body></html>`;
  saveReportHtml(sessionId,html);return html;
}

module.exports={buildAnalyticsReport,analysisHtml,REPORT_DIR,reportFileName};
